import {ArrowUpRight} from 'lucide-react'
export function CategoryCard({title,subtitle,image,className=''}:{title:string;subtitle:string;image:string;className?:string}){return <article className={`group relative min-h-[275px] overflow-hidden rounded-xl bg-slate-800 shadow-card ${className}`}><img src={image} alt="" className="absolute inset-0 h-full w-full object-cover transition duration-500 group-hover:scale-105"/><div className="absolute inset-0 bg-gradient-to-t from-navy/90 via-navy/15 to-transparent"/><div className="absolute inset-x-0 bottom-0 flex items-end justify-between p-6 text-white"><div><h3 className="text-xl font-bold">{title}</h3><p className="mt-1 text-sm text-white/80">{subtitle}</p></div><span className="grid h-10 w-10 place-items-center rounded-full bg-white/20 backdrop-blur"><ArrowUpRight size={20}/></span></div></article>}
